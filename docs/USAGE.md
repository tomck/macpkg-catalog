# Usage
Generate with `python3 -m macpkg_catalog generate --refresh --output dist`; query the downloaded SQLite snapshot with `lookup`, `relations`, or `search`. Static Pages cannot provide arbitrary server-side queries.
